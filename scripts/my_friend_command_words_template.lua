-- Copy this file to my_friend_command_words_<author>.lua and add the module
-- name to my_friend_command_word_packs.lua.
-- Do not reuse an existing id unless you are deliberately adding aliases to
-- that command.  New ids require matching behavior code in the project.
return {
    { id = "existing_command_id",
        zh = { "中文关键词" },
        en = { "english keyword" } },
}
