-- English keyword pack template. Copy to a language-specific module and add
-- its base name to my_friend_command_word_packs.lua.
return {
    { id = "existing_command_id", en = { "english keyword" } },
}
