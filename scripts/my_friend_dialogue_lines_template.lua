-- Copy this file to my_friend_dialogue_lines_<author>.lua and add the module
-- name to my_friend_dialogue_packs.lua.
-- Keep each line as { "中文", "English" }.  The key must already be used by
-- the companion code, unless the pack is accompanied by matching behavior.
return {
    lines = {
        existing_line_key = {
            { "中文台词", "English line" },
        },
    },
    replies = {
        existing_reply_key = { "中文回复", "English reply" },
    },
}
