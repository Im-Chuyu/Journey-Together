-- Optional command keyword packs.
--
-- Add a base module name here when accepting a contributor's keyword files:
--     return { "my_friend_command_words_example" }
-- The loader selects my_friend_command_words_example_<language>.lua and then
-- falls back to the Chinese file. Each module returns command entries with
-- the same shape as the selected language file.
return {}
