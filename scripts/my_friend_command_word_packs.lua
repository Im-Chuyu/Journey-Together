-- Optional command keyword packs.
--
-- Add a module name here when accepting a contributor's keyword file:
--     return { "my_friend_command_words_example" }
-- The module must return an array of command entries with the same shape as
-- my_friend_command_words.lua.  Core command ids still have to be handled by
-- the program; this file is intended for new aliases or compatible commands.
return {}
