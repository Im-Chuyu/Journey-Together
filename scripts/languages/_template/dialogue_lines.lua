-- Copy this folder to scripts/languages/<code>/.
-- Each key must already exist in the Chinese dialogue file.
return {
    lines = {
        greeting = {
            { "One dialogue line" },
        },
    },
    replies = {
        follow_ok = { "One reply" },
    },
}
