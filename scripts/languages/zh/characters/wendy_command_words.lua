-- Wendy-only companion commands. Keep ids stable; the wheel reads these
-- definitions alongside the shared command_words.lua catalogue.
return {
    {id = "wendy_recall", keywords = {"收回阿比盖尔"}, label = "收回阿比盖尔"},
}
